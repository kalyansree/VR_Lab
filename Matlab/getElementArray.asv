function arr = getElementArray(str)
    n = length(str) / 4;
    arr = zeros(n, 7);
    doubleCell = str2double(strsplit(str, ' '));
    for i=1:n
        arr(i,1) = i;
        arr(i,2:3) = 
    end
    
end