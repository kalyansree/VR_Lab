function finalString = getDeformedCoordString(node)
    finalString = '';
    index = 1;
    for i = 1:size(node,1)
        finalString(index) = node{i,1};
        index = index + 1;
        finalString(
        for j = 2:size(node,2)
            tempStr = num2str(node{i,j), '%.4';
        
    end


end